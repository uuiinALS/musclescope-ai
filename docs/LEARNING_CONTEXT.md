# 跨对话学习上下文

## 学习者起点

- 北京交通大学计算机科学与技术，大二。
- C学过但指针和内存薄弱。
- Python看过变量、判断、循环、函数、类、异常，但缺少独立编码。
- 本学期同步学习数据结构、计算机系统和Java OOP。
- 近期目标是建立基础并争取2027寒假或暑期实习。

## 辅导规则

1. 默认按接近零工程基础教学，但不把学习者当作没有思考能力。
2. 先解释“为什么学、在项目中放在哪里、输入输出是什么”。
3. 一次只布置一个可在60—120分钟完成的任务。
4. 学习者先写；辅导者优先给提示、解释报错和审查，不直接生成整份作业。
5. 手机输入造成的缩进、转义和换行问题只指出，不作为概念错误深究。
6. 每个任务都给验收标准，并更新Issue打卡。
7. Java服务于后端实习，Python服务于视觉/生物力学项目，C和数据结构优先配合学校。
8. 不提前堆砌FastAPI、Spring Boot、Redis和深度学习。

## 当前进度

- Day 1已完成：add、maximum、distance。
- Day 2已提交辅导修正版 exercises/day02_files.py；需确认文件存在/不存在两种情况的实际运行及解释。
- Day 3已提交原稿并上传辅导修正版 exercises/day03_json.py；尚未认定独立通过。
- Day 3原稿已会 json.load/json.dump、编码与缩进参数；需补字典键值遍历、复制后删除键、单个完整JSON对象写入、逐点访问与KeyError处理。
- 辅导补充了删除副本顶层键、逐点输出、重新读取副本及KeyError处理。不能将这些补充视为学习者已独立掌握。
- 本轮修正版已做静态审查与远端回读；尚无Python运行验证。
- Day 4已提交parse_point原稿；辅导修正版已更新week01_tasks.py，并新增day04_main.py调用示例。尚未独立验收。
- Day 4重点：data与dict的区别；KeyError、ValueError与TypeError；except只执行首个匹配分支；先检查类型再转换；函数抛异常、调用者显示；__name__与导入；Python引用和C指针的区别。
- 当前终端启动失败，Day 4未实际运行测试；上传与回读不等于执行验证。
- Day 5学习者已自行修正or、分母括号与return；calculate_angle核心逻辑静态审查通过，已上传。保留两个向量同时反向的正确实现。尚未实际运行验证。
- Day 5待巩固：零向量没有方向，夹角未定义，不应返回0度；运行验收合并进Day 6。
- Day 6已提交测试原稿；修正独立函数误用self.calculate_angle与move缺少test_前缀，补充90度、0度、120度、交换端点和缩放性质，共10个测试，保存为tests/test_exercises.py。
- 2026-09-11：辅导者通过Node调用Python 3.14.3，在内存模块中运行从GitHub读取的练习源码与上传的测试文本。正确版10/10通过；只在内存中把uv/(u*v)改为uv/u*v，45度与120度两项失败；恢复后10/10通过。未更改远端公式，未运行全仓库测试或验证本地目录导入。
- Day 6测试代码已验证，可进入Day 7；测试执行与补充由辅导者完成，不视为学习者已独立掌握全部用例设计。
- Day 7第一轮原稿已提交，调用顺序基本正确；修正from导入、as f、json.load、data键访问及具名结果字典，辅导补充main/入口判断和异常分支。已上传exercises/day07_main.py。
- Day 7实际验证：临时目录真实文件/脚本执行，正常、文件不存在、JSON错误、缺少点、缺少坐标、类型错误、重合点7类场景，分别在有/无旧输出下共14次运行通过；非法输入不新建或覆盖输出。10个角度测试在实际目录发现执行全部通过。初次测试的子进程编码设置不一致已在测试运行器修正。
- 当前范围：普通有限数值、小型对象格式输入；未覆盖所有顶层类型、NaN/Infinity/极大数值、写入权限或磁盘错误。异常提示后目前正常退出，退出码改进后续补充。不得宣称生产级完备。
- 下一步Day 7第二轮：学命令行输入输出参数，随后整理输入输出/校验/计算职责与独立复述；本轮辅导补充不视为完全独立实现。
- 用户授权未来按需参考GitHub开源项目并用AI辅助开发；应先确认具体需求与许可，记录来源、自己的改动和验证。架构与关键算法要求学习者能解释并修改，不把第三方或AI实现表述为独立原创。
- 学习方式改为小功能驱动：先明确输入输出/模块职责/验收样例，旧知识直接应用，新知识按需补；从正常流程逐步增加异常与测试，不重复整章基础视频。
- 用户明确要求辅导者持续制定蓝图并布置递进任务，验收后主动衔接下一单元；成熟后逐步转向需求与里程碑驱动。项目目标为可独立解释、修改、测试、展示的求职/升学项目，不以完成短练习或AI代写为目标。
- 蓝图：M0完整角度分析器（读入/校验/计算/输出/自动测试）→M1视频关键点与单动作计数（时序/遮挡/置信度）→M2产品化（接口/界面/记录/部署）→M3可信评估（标注集/指标/对照/误差分析/复现实验与展示）。具体架构随需求演进，普通视频不承诺直接测量真实肌肉激活。
- 教学方式：当前每次60—120分钟明确交付，随后逐步合并成独立小功能；每单元解释输入输出、文件职责、验证方法，避免重复基础抄写。项目预算以近期沟通的每周约8小时为试行安排，旧ROADMAP的3小时预算已不适用。
- 验收标准：允许查API名字；能独立组织流程、运行正常/缺失数据样例、解释错误并完成小变式。上传修正版不等于独立验收通过。
- 学习者希望维持项目投入和逐渐提高难度；不要仅以短代码抄写作为每日完成标准，应逐步加入复写、调试、变式和集成。

## 新对话开始提示词

请先阅读仓库的 README.md、docs/LEARNING_CONTEXT.md、docs/ROADMAP.md、docs/WEEK_01.md 和 Issue #1，然后从当前未完成的第一项继续辅导。不要重做已完成任务；先说明今天为什么学、它在项目架构中的位置，再给练习和验收标准。